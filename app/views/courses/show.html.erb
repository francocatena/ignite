<div class="page-header">
  <h1><%= @course.name %></h1>
</div>

<blockquote>
  <%= simple_format @course.notes %>
</blockquote>

<nav class="form-actions">
  <%= link_to_edit edit_course_path(@course) %> |
  <%= link_to t('view.courses.add_lesson'), new_course_lesson_path(@course) %> |
  <%= link_to t('label.list'), courses_path %>
</nav>