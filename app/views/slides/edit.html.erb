<%= content_for :form_actions do %>
  <%= link_to_show course_lesson_path(@lesson.course, @lesson, anchor: @slide.anchor) %> |
  <%= link_to t('label.list'), lesson_slides_path(@lesson) %>
<% end %>

<%= render 'form' %>